packages:
  - "tests/fixtures/*"
  - "." # This is needed for changesets to detect the root package

allowBuilds:
  dprint: true

dedupePeers: true

overrides:
  rolldown: ^1.0.0

shellEmulator: true
